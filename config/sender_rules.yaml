# Rule-based classification. Unmatched messages are classified as "other".

newsletter:
  patterns:
    - from_contains: alphasignal
  domains:
    - tldrnewsletter.com

community:
  domains:
    - lists.boost.org
  addresses: []
